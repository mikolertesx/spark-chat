{
  "name": "zenzen-chat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "npx lerna exec npm install",
    "prepare-commit": "echo 'Linting frontend project' && cd ./packages/frontend && npx lint-staged",
    "start:backend": "docker compose -f \"docker-compose.yaml\" up -d --build",
    "start:frontend": "echo Frontend is not configured",
    "test": "echo Tests are not configured",
    "setup": "node ./setup/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^7.1.0",
    "lint-staged": "^13.2.3"
  },
  "dependencies": {
    "prompt-sync": "^4.2.0",
    "yargs": "^17.7.2"
  }
}
